# Node modules
node_modules/
dist/
coverage/

# Python
venv/
__pycache__/
*.pyc
.env
.pytest_cache/

# System
.DS_Store
Thumbs.db

# Project specific
backend/requirements.txt # User requested to generate/update this, but usually we don't ignore it. Wait, "Generate/Update requirements.txt" was the task. So I should NOT ignore it.
# The previous plan said "Generate/Update requirements.txt for backend", so I should make sure it is NOT ignored.
